-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
ruff>=0.4
pip-audit>=2.7
# Real Genblaze SDK (pure-Python, no credentials) so the SDK-boundary contract
# test in tests/integration/test_genblaze_contract.py actually RUNS in CI —
# guarding the adapter against Genblaze API drift.
genblaze-core>=0.3.4,<0.4
